[project]
name = "postgenius"
version = "0.1.0"
description = "AI-powered social media post generation with FastAPI and Jinja2"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "jinja2>=3.1.6",
]
